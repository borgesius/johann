# Tiny Smoke Brief

Create the smallest believable code repo you can without turning it into nonsense.

The goal is not product depth. The goal is to prove the worker loop can:
- inspect the repo
- make a plan
- write files
- leave behind something coherent enough to judge

Put the implementation file under `src/` so the repo shape is predictable.
