# Architecture

This seed starts with:

- a control plane server
- a service graph model
- a simulated fleet
- a simple operator console

Later stages should add a richer planning engine, execution runtime, queueing, verification, and rollback handling without rewriting the basic server and UI split.
